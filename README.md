# Yosh-Machine
Yosh's Machine

This is type of computer with value range 0 to ∞ and there memory and input device and output device, if you command/send the value to the memory and then you command/accept using input device or others (it decide by creature/human/progam (not autonomous)) it will send to the where device that are determined by the size of value you send; and can Increment and decrement the memory and or other methods; so the memory is empty for ready other value, and also more explanation; only 1 value per memory, and also memory have no fixed maximum size and also expandable so if you send value bigger than the memory capacity it just the biggest value and also if you send more value if it's the memory full occupied it can't, it expandable may use 3d printer or quantum and exotic builder or distribution or other; and the engine for the expandable are progammable, and recommendations like use automation or connecting with others, this is looks like multiple buttons but to click where the button is determined how big is the value.
